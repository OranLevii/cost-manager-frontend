import { createTheme } from "@mui/material/styles";

/**
 * Material-UI theme configuration
 * Defines the application's color scheme and styling defaults
 * Currently set to light mode
 */
export const theme = createTheme({
  palette: { mode: "light" },
});
